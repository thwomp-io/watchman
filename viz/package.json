{
  "name": "the toolkit-viz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "D3 server-side SVG renderer for the toolkit. Python shells out to render.js; output is static SVG embedded in the Obsidian corpus. Quarantined JS toolchain — see ../README / CHANGELOG.",
  "license": "MIT",
  "scripts": {
    "render": "node render.js"
  },
  "dependencies": {
    "d3": "^7.9.0",
    "d3-sankey": "^0.12.3",
    "jsdom": "^24.1.3",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  }
}
